[build]
    command = "npm run build"
    publish = "dist"

# Netlify otherwise serves .webmanifest files as application/octet-stream.
[[headers]]
    for = "/site.webmanifest"
    [headers.values]
        Content-Type = "application/manifest+json"
